# USYD Web Crawler and RAG - Azure Developer CLI Configuration

name: usyd-web-crawler-rag
metadata:
  template: usyd-web-crawler-rag@0.0.1
  
services:
  web:
    project: .
    language: python
    host: containerapp
    
hooks:
  predeploy:
    windows:
      - echo "Building Docker image..."
    posix:
      - echo "Building Docker image..."
  
  postdeploy:
    windows:
      - echo "Running post-deployment validation..."
      - python scripts/validate_deployment.py
    posix:
      - echo "Running post-deployment validation..."
      - python scripts/validate_deployment.py